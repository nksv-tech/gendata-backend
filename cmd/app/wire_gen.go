// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/gen-data/gendata-server/internal/config"
	"github.com/gen-data/gendata-server/internal/server"
	"github.com/gen-data/gendata-server/internal/service"
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(configConfig *config.Config, logger log.Logger) (*kratos.App, func(), error) {
	genDataService := service.NewGenDataService()
	limiter := server.NewRateLimiter(configConfig)
	v := server.NewMiddlewares(configConfig, logger, limiter)
	cMux := server.NewMux(configConfig)
	tlsConfig := server.NewTLS(configConfig)
	grpcServer := server.NewGRPCServer(configConfig, genDataService, v, cMux, logger, tlsConfig)
	httpServer := server.NewHTTPServer(configConfig, genDataService, v, cMux, logger, tlsConfig)
	app := newApp(logger, grpcServer, httpServer)
	return app, func() {
	}, nil
}
